{
  "name": "uptime-guard",
  "private": true,
  "scripts": {
    "build:dashboard": "npm --prefix dashboard run build",
    "dev": "npm run build:dashboard && npm --prefix worker run dev",
    "deploy": "npx wrangler deploy"
  }
}
